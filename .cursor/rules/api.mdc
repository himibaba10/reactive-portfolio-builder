---
description: Next.js server/API and MongoDB data model conventions
globs: web/src/app/api/**/*.{ts,tsx},web/src/lib/server/**/*.{ts,tsx},web/src/lib/db/**/*.{ts,tsx}
alwaysApply: false
---

# Server / API (`web/`)

## Architecture

**Next.js only** — no separate Express app. Auth, portfolio CRUD, and public data live in:

- Route Handlers under `web/src/app/api/`
- and/or Server Actions

## Today

Foundations landed: MongoDB connect helper, User/Portfolio models, auth/slug/section server utilities, `GET /api/health`. Auth and portfolio Route Handlers come next.

## Stack

Next.js Route Handlers + Mongoose + MongoDB Atlas · JWT in httpOnly cookies

## Planned collections

**users:** email (unique), passwordHash, isEmailVerified, verification/reset tokens, deletedAt, timestamps  

**portfolios:** userId (unique), title, slug (unique), status `draft|published`, paletteId, embedded `sections[]` (`id`, `type`, `order`, `visible`, `data`), publishedAt, timestamps

## Planned endpoints (implement when asked)

- Auth: signup, login, logout, verify-email, forgot/reset password, soft-delete account
- Portfolio: get/create (409 if exists), patch, delete, slug availability/change
- Public: published portfolio by slug (for `/{slug}` SSR)

## Rules

- Validate inputs (Zod preferred when shared schemas land)
- Rate-limit auth + slug checks
- Soft-deleted users cannot authenticate; free their slug on account delete
- One portfolio per user enforced at DB unique index + application layer
- Do not reintroduce a separate `api/` Express package
