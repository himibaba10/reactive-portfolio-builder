# Reactive Portfolio Builder API

Express + Mongoose will own auth and portfolio CRUD in later milestones.

Currently exposes `GET /health`.

Read root `AGENTS.md` and `.cursor/rules/api.mdc` before extending this service.
